# pytest.ini
[pytest]
DJANGO_SETTINGS_MODULE = websiteProject.settings
# або ваш_проект.settings, якщо назва проекту інша
python_files = test_*.py *_test.py
addopts = --no-migrations --create-db --reuse-db # Рекомендовані опції для Django